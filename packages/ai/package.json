{
  "name": "@retenia/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI SDK providers, routing by role, versioned prompts, pricing, budgets, keys (interface). Real providers land in sub-phase 7.x.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@retenia/core": "workspace:*"
  },
  "devDependencies": {
    "@retenia/config": "workspace:*",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
