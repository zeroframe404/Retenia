{
  "name": "@retenia/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI provider profiles, role routing, the versioned pricing table, cost log and monthly budget; structured outputs with the validation/repair loop, the versioned prompt registry and the idempotent result cache; the Batch API runner, prompt-caching breakpoints and the pre-submission cost estimator; local providers (Ollama, LM Studio), offline policy and the provider allow-list.",
  "exports": {
    ".": "./src/index.ts",
    "./prompts": "./src/prompts/index.ts",
    "./prompts-bundled": "./src/prompts-bundled.ts",
    "./providers": "./src/providers/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@ai-sdk/anthropic": "4.0.49",
    "@ai-sdk/google": "4.0.64",
    "@ai-sdk/openai-compatible": "3.0.45",
    "@retenia/core": "workspace:*",
    "ai": "7.0.93",
    "p-queue": "^9.3.3",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@retenia/config": "workspace:*",
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
