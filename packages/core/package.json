{
  "name": "@retenia/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain logic. Zero Electron, Node, or AI-provider SDK imports \u2014 depends only on ports.",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@retenia/config": "workspace:*",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
