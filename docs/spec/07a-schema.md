Generated by `pnpm --filter @retenia/db schema:doc` — do not edit by hand.

# Data schema (v1)

The SQLite schema of `packages/db`, listed from a freshly migrated database so it matches the shipped migrations exactly. It implements the data layer of [`07-architecture.md`](07-architecture.md) §5, the memory model of [`02-memory-system.md`](02-memory-system.md) §14, the activity envelope of [`03-activities.md`](03-activities.md) §7 and the path schemas of [`04-path-generation.md`](04-path-generation.md) §8.

## Conventions

- **Ids** are UUIDv7 strings (`@retenia/core` `createUuidV7Generator`). Every domain table has `CHECK (length(id) = 36 AND substr(id, 15, 1) = '7')`, so a v4 id or an integer is rejected at the boundary.
- **Audit columns** on every domain table: `created_at`, `updated_at` (Unix ms), `deleted_at` (soft delete — nothing issues `DELETE`), `device_id`, `version` (starts at 1, incremented per update). `updated_at >= created_at` and `version >= 1` are CHECKed.
- **JSON** lives in `TEXT` columns guarded by `json_valid()`; where the shape is fixed, also by `json_type() = 'object'`/`'array'`. Drizzle parses/stringifies them (`text(…, { mode: 'json' })`).
- **Enumerations** are `TEXT`/`INTEGER` columns with `CHECK (… IN (…))`; the allowed values are exported as constants from `@retenia/db/schema` (`IMPORTANCE_LEVELS`, `CARD_STATES`, `REVIEW_CONTEXTS`, …).
- **Foreign keys** are declared everywhere a column holds another row's id and are enforced (`PRAGMA foreign_keys = ON`). No cascades: rows are soft-deleted, never removed.
- **Live-only unique indexes** (`… WHERE deleted_at IS NULL`) let a soft-deleted key be reused (`settings.key`, `scheduler_profiles.scope`, `streaks.kind`, `achievements.key`, `cards(item_id, template)`).
- **FSRS parity**: the nine `cards` columns `due, stability, difficulty, scheduled_days, learning_steps, reps, lapses, state, last_review` and the nine `review_logs` columns `rating, state, due, stability, difficulty, elapsed_days, scheduled_days, learning_steps, review` are `ts-fsrs`'s `Card`/`ReviewLog` verbatim. In `review_logs`, `state/due/stability/difficulty` are the values *before* the review (what `02-memory-system.md` §14 sketches as `*_before`). `elapsed_days` is deliberately absent from `cards` (`ts-fsrs@6` drops it).
- **`review_logs` is append-only**: `CHECK (updated_at = created_at AND version = 1)` rejects any update except setting `deleted_at` when the parent card is soft-deleted.
- **Sync-ready**: UUIDv7 ids, soft deletes, `device_id`/`version` per row, an `outbox` that stays empty in v1, no `AUTOINCREMENT`.

## Opening the database

`openDatabase(path, { encryptionKey?, driver?, loadVec?, readonly?, busyTimeoutMs? })` (`src/open-database.ts`) applies, in order:

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
PRAGMA cache_size = -64000;
PRAGMA temp_store = MEMORY;
```

then loads sqlite-vec with `db.loadExtension(getLoadablePath())` and wraps the connection in Drizzle (`drizzle-orm` 0.45, `better-sqlite3` 13). Both drivers ship N-API prebuilds for win32/darwin/linux inside the npm package (no build step; `allowBuilds` lists them as `false` on purpose). `better-sqlite3-multiple-ciphers` is an optional dependency selected by `driver: 'better-sqlite3-multiple-ciphers'` or automatically when `encryptionKey` is given; the key comes from `safeStorage` in the main process and is applied as `PRAGMA cipher = 'chacha20'; PRAGMA key = …` before any other statement. Passing a key to the plain driver is an error rather than a silent no-op.

Packaging note (Windows first): the `.node` binaries of both drivers and `sqlite-vec-windows-x64/vec0.dll` must be `asarUnpack`ed by electron-builder, and sqlite-vec resolves its binary with `import.meta.resolve`, so the main bundle has to keep it external.

## Migrations

`packages/db/migrations/NNNN_name.sql` — `drizzle-kit generate` output for the Drizzle tables, plus hand-written files (`drizzle-kit generate --custom`) for what Drizzle cannot express. `migrate(db)` (`src/migrator.ts`) runs on every app start: it creates `_migrations` if needed, applies each pending file inside its own transaction (a failing statement rolls the whole file back) and records `name`, `sha256`, `applied_at`, `duration_ms`.

**Applied migrations are immutable.** The migrator compares every recorded hash with the file on disk and refuses to start if one changed, refuses a database migrated further than the build knows (no silent downgrade), and refuses a pending file that sorts before an applied one. To change the schema, add a new file — never edit or delete an existing one (`docs/spec/00-conventions.md`; the repo's Claude hooks block edits under `migrations/`).

| # | Migration | SHA-256 (prefix) | Contents |
|---|---|---|---|
| 0 | `0000_domain_schema` | `ec569f2a7be5` | All Drizzle tables, indexes, foreign keys and CHECKs. |
| 1 | `0001_fts5_vec0_seed` | `b88c31b72a8f` | `chunks_fts` (FTS5, `unicode61 remove_diacritics 2`) + sync triggers, `embeddings` (vec0, `float[768]`, partition `source_id`), the five `importance_levels` rows. |

## Tables

| Table | Group | Columns | Foreign keys | Indexes | Checks |
|---|---|---|---|---|---|
| `blobs` | Source library | 12 | 0 | 1 | 6 |
| `sources` | Source library | 15 | 1 | 2 | 6 |
| `source_units` | Source library | 15 | 2 | 1 | 7 |
| `chunks` | Source library | 17 | 2 | 3 | 7 |
| `annotations` | Source library | 15 | 2 | 2 | 7 |
| `chunks_fts` | Search indexes (virtual tables) | 4 | 0 | 0 | 0 |
| `embeddings` | Search indexes (virtual tables) | 5 | 0 | 0 | 0 |
| `paths` | Learning paths | 15 | 0 | 1 | 8 |
| `path_versions` | Learning paths | 13 | 1 | 1 | 8 |
| `sections` | Learning paths | 12 | 1 | 1 | 6 |
| `modules` | Learning paths | 14 | 1 | 1 | 8 |
| `lessons` | Learning paths | 24 | 2 | 3 | 16 |
| `activities` | Learning paths | 20 | 1 | 3 | 14 |
| `exams` | Exams and item bank | 17 | 1 | 2 | 12 |
| `item_bank` | Exams and item bank | 14 | 3 | 3 | 6 |
| `exam_items` | Exams and item bank | 14 | 3 | 2 | 7 |
| `exam_attempts` | Exams and item bank | 15 | 1 | 1 | 10 |
| `importance_levels` | Memory system | 14 | 0 | 1 | 11 |
| `scheduler_profiles` | Memory system | 20 | 0 | 1 | 11 |
| `knowledge_items` | Memory system | 18 | 3 | 5 | 10 |
| `cards` | Memory system | 23 | 2 | 5 | 14 |
| `lesson_sessions` | Sessions, attempts and review log | 16 | 1 | 2 | 10 |
| `attempts` | Sessions, attempts and review log | 23 | 5 | 5 | 14 |
| `review_logs` | Sessions, attempts and review log | 21 | 2 | 3 | 14 |
| `jobs` | Infrastructure | 23 | 1 | 4 | 9 |
| `ai_calls` | Infrastructure | 25 | 1 | 4 | 12 |
| `settings` | Infrastructure | 8 | 0 | 1 | 5 |
| `outbox` | Infrastructure | 14 | 0 | 2 | 7 |
| `xp_events` | Gamification | 13 | 0 | 2 | 7 |
| `streaks` | Gamification | 16 | 0 | 1 | 10 |
| `achievements` | Gamification | 11 | 0 | 1 | 7 |
| `_migrations` | Migration bookkeeping | 4 | 0 | 0 | 0 |

## Source library

What the user loads, how it is split for citation and retrieval, what they mark on it (`src/schema/library.ts`).

### `blobs`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `sha256` | text | no |  |  |
| `mime` | text | no |  |  |
| `bytes` | integer | no |  |  |
| `ext` | text | yes |  |  |
| `original_name` | text | yes |  |  |
| `meta` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `blobs_sha256` UNIQUE (`sha256`)

Checks:

- `blobs_sha256_hex`: `length(sha256) = 64 AND sha256 = lower(sha256)`
- `blobs_bytes_nonnegative`: `bytes >= 0`
- `blobs_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `blobs_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `blobs_version_positive`: `version >= 1`
- `blobs_updated_after_created`: `updated_at >= created_at`

### `sources`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `kind` | text | no |  |  |
| `title` | text | no |  |  |
| `origin_uri` | text | yes |  |  |
| `blob_sha256` | text | yes |  | → `blobs.sha256` |
| `status` | text | no | `'pending'` |  |
| `language` | text | yes |  |  |
| `meta` | text | yes |  |  |
| `error` | text | yes |  |  |
| `ingested_at` | integer | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `sources_blob` (`blob_sha256`)
- `sources_status` (`status`)

Checks:

- `sources_kind`: `kind IN ('pdf', 'docx', 'epub', 'pptx', 'markdown', 'text', 'image', 'audio', 'video', 'youtube', 'web')`
- `sources_status`: `status IN ('pending', 'processing', 'ready', 'failed')`
- `sources_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `sources_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `sources_version_positive`: `version >= 1`
- `sources_updated_after_created`: `updated_at >= created_at`

### `source_units`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `source_id` | text | no |  | → `sources.id` |
| `kind` | text | no |  |  |
| `ordinal` | integer | no |  |  |
| `label` | text | yes |  |  |
| `t_start` | integer | yes |  |  |
| `t_end` | integer | yes |  |  |
| `text` | text | yes |  |  |
| `blob_sha256` | text | yes |  | → `blobs.sha256` |
| `meta` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `source_units_source_ordinal` (`source_id`, `ordinal`)

Checks:

- `source_units_kind`: `kind IN ('page', 'slide', 'section', 'keyframe', 'segment')`
- `source_units_ordinal_nonnegative`: `ordinal >= 0`
- `source_units_time_range`: `t_start IS NULL OR t_end IS NULL OR t_end >= t_start`
- `source_units_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `source_units_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `source_units_version_positive`: `version >= 1`
- `source_units_updated_after_created`: `updated_at >= created_at`

### `chunks`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `source_id` | text | no |  | → `sources.id` |
| `unit_id` | text | yes |  | → `source_units.id` |
| `ordinal` | integer | no |  |  |
| `text` | text | no |  |  |
| `char_start` | integer | no |  |  |
| `char_end` | integer | no |  |  |
| `token_count` | integer | no |  |  |
| `hash` | text | no |  |  |
| `heading_path` | text | yes |  |  |
| `context` | text | yes |  |  |
| `locator` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `chunks_unit` (`unit_id`)
- `chunks_hash` (`hash`)
- `chunks_source_ordinal` (`source_id`, `ordinal`)

Checks:

- `chunks_char_range`: `char_start >= 0 AND char_end >= char_start`
- `chunks_token_count_nonnegative`: `token_count >= 0`
- `chunks_hash_hex`: `length(hash) = 64`
- `chunks_locator_json`: `locator IS NULL OR (json_valid(locator) AND json_type(locator) = 'object')`
- `chunks_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `chunks_version_positive`: `version >= 1`
- `chunks_updated_after_created`: `updated_at >= created_at`

Triggers:

- `chunks_fts_ad`: AFTER DELETE ON chunks
- `chunks_fts_ai`: AFTER INSERT ON chunks
- `chunks_fts_au`: AFTER UPDATE OF source_id, text, heading_path, deleted_at ON chunks

### `annotations`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `source_id` | text | no |  | → `sources.id` |
| `unit_id` | text | yes |  | → `source_units.id` |
| `kind` | text | no |  |  |
| `anchor` | text | no |  |  |
| `quote` | text | yes |  |  |
| `note` | text | yes |  |  |
| `color` | text | yes |  |  |
| `t_start` | real | yes |  |  |
| `t_end` | real | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `annotations_unit` (`unit_id`)
- `annotations_source` (`source_id`)

Checks:

- `annotations_kind`: `kind IN ('highlight', 'note', 'region', 'clip')`
- `annotations_anchor_json`: `json_valid(anchor) AND json_type(anchor) = 'object'`
- `annotations_time_range`: `t_start IS NULL OR t_end IS NULL OR t_end >= t_start`
- `annotations_t_start_nonnegative`: `t_start IS NULL OR (t_start >= 0 AND t_start <= 9007199254740991)`
- `annotations_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `annotations_version_positive`: `version >= 1`
- `annotations_updated_after_created`: `updated_at >= created_at`

## Search indexes (virtual tables)

FTS5 over `chunks` and the sqlite-vec store. Created by the raw-SQL migration, queried through `src/search.ts`. `chunks_fts` is kept in sync by triggers, including on soft delete; `embeddings` is derived data that the embedding job deletes and rebuilds, so it carries no audit columns (`vec0` has no NOT NULL/CHECK).

### `chunks_fts`

Virtual table — DDL as shipped:

```sql
CREATE VIRTUAL TABLE `chunks_fts` USING fts5(
	`chunk_id` UNINDEXED,
	`source_id` UNINDEXED,
	`text`,
	`heading_path`,
	tokenize = 'unicode61 remove_diacritics 2'
)
```

### `embeddings`

Virtual table — DDL as shipped:

```sql
CREATE VIRTUAL TABLE embeddings USING vec0(
	id TEXT PRIMARY KEY,
	source_id TEXT PARTITION KEY,
	chunk_id TEXT,
	model_id TEXT,
	embedding FLOAT[768]
)
```

## Learning paths

A path, its frozen versions, and the version-owned tree sections → modules → lessons → activities (`src/schema/paths.ts`).

### `paths`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `title` | text | no |  |  |
| `language` | text | no |  |  |
| `level` | text | yes |  |  |
| `goal` | text | yes |  |  |
| `target_date` | text | yes |  |  |
| `status` | text | no | `'draft'` |  |
| `active_version` | integer | yes |  |  |
| `source_ids` | text | no | `'[]'` |  |
| `settings` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `paths_status` (`status`)

Checks:

- `paths_status`: `status IN ('draft', 'generating', 'active', 'completed', 'archived')`
- `paths_active_version_positive`: `active_version IS NULL OR active_version >= 1`
- `paths_target_date_iso`: `target_date IS NULL OR target_date GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'`
- `paths_source_ids_json`: `json_valid(source_ids) AND json_type(source_ids) = 'array'`
- `paths_settings_json`: `settings IS NULL OR (json_valid(settings) AND json_type(settings) = 'object')`
- `paths_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `paths_version_positive`: `version >= 1`
- `paths_updated_after_created`: `updated_at >= created_at`

### `path_versions`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `path_id` | text | no |  | → `paths.id` |
| `number` | integer | no |  |  |
| `spec` | text | no |  |  |
| `knowledge_graph` | text | yes |  |  |
| `manifest` | text | yes |  |  |
| `diff` | text | yes |  |  |
| `frozen_at` | integer | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `path_versions_path_number` UNIQUE (`path_id`, `number`)

Checks:

- `path_versions_number_positive`: `number >= 1`
- `path_versions_spec_json`: `json_valid(spec) AND json_type(spec) = 'object'`
- `path_versions_knowledge_graph_json`: `knowledge_graph IS NULL OR (json_valid(knowledge_graph) AND json_type(knowledge_graph) = 'object')`
- `path_versions_manifest_json`: `manifest IS NULL OR (json_valid(manifest) AND json_type(manifest) = 'object')`
- `path_versions_diff_json`: `diff IS NULL OR (json_valid(diff) AND json_type(diff) = 'object')`
- `path_versions_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `path_versions_version_positive`: `version >= 1`
- `path_versions_updated_after_created`: `updated_at >= created_at`

### `sections`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `path_version_id` | text | no |  | → `path_versions.id` |
| `ordinal` | integer | no |  |  |
| `spec_id` | text | no |  |  |
| `title` | text | no |  |  |
| `unlock_rule` | text | yes |  |  |
| `xp_reward` | integer | no | `0` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `sections_version_ordinal` (`path_version_id`, `ordinal`)

Checks:

- `sections_ordinal_nonnegative`: `ordinal >= 0`
- `sections_xp_nonnegative`: `xp_reward >= 0`
- `sections_unlock_rule_json`: `unlock_rule IS NULL OR (json_valid(unlock_rule) AND json_type(unlock_rule) = 'object')`
- `sections_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `sections_version_positive`: `version >= 1`
- `sections_updated_after_created`: `updated_at >= created_at`

### `modules`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `section_id` | text | no |  | → `sections.id` |
| `ordinal` | integer | no |  |  |
| `spec_id` | text | no |  |  |
| `title` | text | no |  |  |
| `objectives` | text | no | `'[]'` |  |
| `diagnostic_item_ids` | text | no | `'[]'` |  |
| `unlock_rule` | text | yes |  |  |
| `xp_reward` | integer | no | `0` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `modules_section_ordinal` (`section_id`, `ordinal`)

Checks:

- `modules_ordinal_nonnegative`: `ordinal >= 0`
- `modules_xp_nonnegative`: `xp_reward >= 0`
- `modules_objectives_json`: `json_valid(objectives) AND json_type(objectives) = 'array'`
- `modules_diagnostic_item_ids_json`: `json_valid(diagnostic_item_ids) AND json_type(diagnostic_item_ids) = 'array'`
- `modules_unlock_rule_json`: `unlock_rule IS NULL OR (json_valid(unlock_rule) AND json_type(unlock_rule) = 'object')`
- `modules_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `modules_version_positive`: `version >= 1`
- `modules_updated_after_created`: `updated_at >= created_at`

### `lessons`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `module_id` | text | no |  | → `modules.id` |
| `ordinal` | integer | no |  |  |
| `spec_id` | text | no |  |  |
| `kind` | text | no | `'core'` |  |
| `parent_lesson_id` | text | yes |  | → `lessons.id` |
| `title` | text | no |  |  |
| `status` | text | no | `'pending'` |  |
| `objectives` | text | no | `'[]'` |  |
| `concept_ids` | text | no | `'[]'` |  |
| `prerequisite_lesson_ids` | text | no | `'[]'` |  |
| `estimated_minutes` | integer | yes |  |  |
| `theory` | text | yes |  |  |
| `citations` | text | no | `'[]'` |  |
| `qa` | text | yes |  |  |
| `remediation` | text | yes |  |  |
| `unlock_rule` | text | yes |  |  |
| `xp_reward` | integer | no | `0` |  |
| `completed_at` | integer | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `lessons_status` (`status`)
- `lessons_parent` (`parent_lesson_id`)
- `lessons_module_ordinal` (`module_id`, `ordinal`)

Checks:

- `lessons_kind`: `kind IN ('core', 'remediation', 'reinforcement', 'checkpoint')`
- `lessons_status`: `status IN ('pending', 'generating', 'ready', 'failed')`
- `lessons_ordinal_nonnegative`: `ordinal >= 0`
- `lessons_estimated_minutes_positive`: `estimated_minutes IS NULL OR estimated_minutes >= 0`
- `lessons_xp_nonnegative`: `xp_reward >= 0`
- `lessons_objectives_json`: `json_valid(objectives) AND json_type(objectives) = 'array'`
- `lessons_concept_ids_json`: `json_valid(concept_ids) AND json_type(concept_ids) = 'array'`
- `lessons_prerequisites_json`: `json_valid(prerequisite_lesson_ids) AND json_type(prerequisite_lesson_ids) = 'array'`
- `lessons_theory_json`: `theory IS NULL OR (json_valid(theory) AND json_type(theory) = 'object')`
- `lessons_citations_json`: `json_valid(citations) AND json_type(citations) = 'array'`
- `lessons_qa_json`: `qa IS NULL OR (json_valid(qa) AND json_type(qa) = 'object')`
- `lessons_remediation_json`: `remediation IS NULL OR (json_valid(remediation) AND json_type(remediation) = 'object')`
- `lessons_unlock_rule_json`: `unlock_rule IS NULL OR (json_valid(unlock_rule) AND json_type(unlock_rule) = 'object')`
- `lessons_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `lessons_version_positive`: `version >= 1`
- `lessons_updated_after_created`: `updated_at >= created_at`

### `activities`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `lesson_id` | text | yes |  | → `lessons.id` |
| `ordinal` | integer | yes |  |  |
| `type` | text | no |  |  |
| `family` | text | no |  |  |
| `schema_version` | integer | no | `1` |  |
| `lang` | text | no |  |  |
| `bloom` | text | yes |  |  |
| `difficulty` | integer | yes |  |  |
| `concept_ids` | text | no | `'[]'` |  |
| `misconception_ids` | text | no | `'[]'` |  |
| `config` | text | no |  |  |
| `grading` | text | no |  |  |
| `status` | text | no | `'ready'` |  |
| `source_refs` | text | no | `'[]'` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `activities_status` (`status`)
- `activities_type` (`type`)
- `activities_lesson_ordinal` (`lesson_id`, `ordinal`)

Checks:

- `activities_family`: `family IN ('choice', 'text_input', 'cloze', 'long_text', 'pairs', 'ordering', 'categorize', 'image_target', 'text_mark', 'scale', 'speech', 'dialogue', 'branching', 'media_checkpoints', 'code', 'math', 'graph', 'grid_game', 'arcade', 'cards', 'disclosure', 'draw', 'simulation')`
- `activities_status`: `status IN ('ready', 'pending_media', 'needs_review', 'rejected')`
- `activities_bloom`: `bloom IS NULL OR bloom IN ('remember', 'understand', 'apply', 'analyze', 'evaluate', 'create')`
- `activities_difficulty_range`: `difficulty IS NULL OR (difficulty >= 1 AND difficulty <= 5)`
- `activities_schema_version_positive`: `schema_version >= 1`
- `activities_ordinal_nonnegative`: `ordinal IS NULL OR ordinal >= 0`
- `activities_concept_ids_json`: `json_valid(concept_ids) AND json_type(concept_ids) = 'array'`
- `activities_misconception_ids_json`: `json_valid(misconception_ids) AND json_type(misconception_ids) = 'array'`
- `activities_config_json`: `json_valid(config) AND json_type(config) = 'object'`
- `activities_grading_json`: `json_valid(grading) AND json_type(grading) = 'object'`
- `activities_source_refs_json`: `json_valid(source_refs) AND json_type(source_refs) = 'array'`
- `activities_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `activities_version_positive`: `version >= 1`
- `activities_updated_after_created`: `updated_at >= created_at`

## Exams and item bank

Dated/mock/final/diagnostic exams, their items and attempts, and the generated item bank (`src/schema/exams.ts`).

### `exams`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `title` | text | no |  |  |
| `kind` | text | no |  |  |
| `date` | text | yes |  |  |
| `path_id` | text | yes |  | → `paths.id` |
| `scope` | text | no | `'{}'` |  |
| `blueprint` | text | no | `'[]'` |  |
| `target_retention` | real | no | `0.95` |  |
| `final_window_days` | integer | no | `3` |  |
| `study_days_mask` | integer | no | `127` |  |
| `daily_capacity_minutes` | integer | yes |  |  |
| `status` | text | no | `'planned'` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `exams_path` (`path_id`)
- `exams_status_date` (`status`, `date`)

Checks:

- `exams_kind`: `kind IN ('dated', 'mock', 'final', 'diagnostic')`
- `exams_status`: `status IN ('planned', 'active', 'completed', 'archived')`
- `exams_date_iso`: `date IS NULL OR date GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'`
- `exams_target_retention_range`: `target_retention >= 0.7 AND target_retention <= 0.99`
- `exams_final_window_nonnegative`: `final_window_days >= 0`
- `exams_study_days_mask_range`: `study_days_mask >= 0 AND study_days_mask <= 127`
- `exams_daily_capacity_positive`: `daily_capacity_minutes IS NULL OR daily_capacity_minutes >= 1`
- `exams_scope_json`: `json_valid(scope) AND json_type(scope) = 'object'`
- `exams_blueprint_json`: `json_valid(blueprint) AND json_type(blueprint) = 'array'`
- `exams_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `exams_version_positive`: `version >= 1`
- `exams_updated_after_created`: `updated_at >= created_at`

### `item_bank`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `activity_id` | text | no |  | → `activities.id` |
| `path_version_id` | text | yes |  | → `path_versions.id` |
| `module_id` | text | yes |  | → `modules.id` |
| `usage` | text | no | `'[]'` |  |
| `difficulty_logit` | real | no | `0` |  |
| `discrimination_hint` | real | yes |  |  |
| `exposure` | integer | no | `0` |  |
| `stats` | text | no | `'{}'` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `item_bank_version` (`path_version_id`)
- `item_bank_module` (`module_id`)
- `item_bank_activity` (`activity_id`)

Checks:

- `item_bank_exposure_nonnegative`: `exposure >= 0`
- `item_bank_usage_json`: `json_valid(usage) AND json_type(usage) = 'array'`
- `item_bank_stats_json`: `json_valid(stats) AND json_type(stats) = 'object'`
- `item_bank_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `item_bank_version_positive`: `version >= 1`
- `item_bank_updated_after_created`: `updated_at >= created_at`

### `exam_items`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `exam_id` | text | no |  | → `exams.id` |
| `ordinal` | integer | no |  |  |
| `activity_id` | text | no |  | → `activities.id` |
| `item_bank_id` | text | yes |  | → `item_bank.id` |
| `form` | text | yes |  |  |
| `topic` | text | yes |  |  |
| `weight` | real | no | `1` |  |
| `time_limit_sec` | integer | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `exam_items_activity` (`activity_id`)
- `exam_items_exam_ordinal` (`exam_id`, `ordinal`)

Checks:

- `exam_items_form`: `form IS NULL OR form IN ('A', 'B')`
- `exam_items_ordinal_nonnegative`: `ordinal >= 0`
- `exam_items_weight_nonnegative`: `weight >= 0`
- `exam_items_time_limit_positive`: `time_limit_sec IS NULL OR time_limit_sec >= 1`
- `exam_items_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `exam_items_version_positive`: `version >= 1`
- `exam_items_updated_after_created`: `updated_at >= created_at`

### `exam_attempts`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `exam_id` | text | no |  | → `exams.id` |
| `mode` | text | no | `'real'` |  |
| `started_at` | integer | no |  |  |
| `finished_at` | integer | yes |  |  |
| `score` | real | yes |  |  |
| `by_topic` | text | no | `'{}'` |  |
| `items` | text | no | `'[]'` |  |
| `readiness_predicted` | real | yes |  |  |
| `affects_scheduling` | integer | no | `1` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `exam_attempts_exam` (`exam_id`)

Checks:

- `exam_attempts_mode`: `mode IN ('real', 'blind', 'preview')`
- `exam_attempts_score_range`: `score IS NULL OR (score >= 0 AND score <= 1)`
- `exam_attempts_readiness_range`: `readiness_predicted IS NULL OR (readiness_predicted >= 0 AND readiness_predicted <= 1)`
- `exam_attempts_affects_scheduling_bool`: `affects_scheduling IN (0, 1)`
- `exam_attempts_finished_after_started`: `finished_at IS NULL OR finished_at >= started_at`
- `exam_attempts_by_topic_json`: `json_valid(by_topic) AND json_type(by_topic) = 'object'`
- `exam_attempts_items_json`: `json_valid(items) AND json_type(items) = 'array'`
- `exam_attempts_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `exam_attempts_version_positive`: `version >= 1`
- `exam_attempts_updated_after_created`: `updated_at >= created_at`

## Memory system

Importance levels, FSRS parameters, knowledge items and cards. FSRS columns mirror `ts-fsrs` 1:1 (`src/schema/memory.ts`).

### `importance_levels`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `name` | text | no |  |  |
| `desired_retention` | real | yes |  |  |
| `max_interval_days` | integer | yes |  |  |
| `order_rank` | integer | no |  |  |
| `postpone_allowed` | integer | no |  |  |
| `new_per_day` | integer | yes |  |  |
| `leech_threshold` | integer | no | `8` |  |
| `leech_action` | text | no |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `importance_levels_name` UNIQUE (`name`)

Checks:

- `importance_levels_name`: `name IN ('urgent', 'high', 'normal', 'maintenance', 'paused')`
- `importance_levels_desired_retention_range`: `desired_retention IS NULL OR (desired_retention >= 0.7 AND desired_retention <= 0.99)`
- `importance_levels_max_interval_positive`: `max_interval_days IS NULL OR max_interval_days >= 1`
- `importance_levels_order_rank_positive`: `order_rank >= 1`
- `importance_levels_postpone_allowed_bool`: `postpone_allowed IN (0, 1)`
- `importance_levels_new_per_day_nonnegative`: `new_per_day IS NULL OR new_per_day >= 0`
- `importance_levels_leech_threshold_positive`: `leech_threshold >= 1`
- `importance_levels_leech_action`: `leech_action IN ('warn', 'warn_rewrite', 'edit', 'suspend', 'none')`
- `importance_levels_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `importance_levels_version_positive`: `version >= 1`
- `importance_levels_updated_after_created`: `updated_at >= created_at`

### `scheduler_profiles`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `scope` | text | no |  |  |
| `algorithm` | text | no | `'fsrs6'` |  |
| `w` | text | no |  |  |
| `decay` | real | yes |  |  |
| `learning_steps` | text | no | `'["1m","10m"]'` |  |
| `relearning_steps` | text | no | `'["10m"]'` |  |
| `enable_fuzz` | integer | no | `1` |  |
| `enable_short_term` | integer | no | `1` |  |
| `maximum_interval` | integer | no | `36500` |  |
| `day_start_hour` | integer | no | `4` |  |
| `trained_at` | integer | yes |  |  |
| `n_reviews` | integer | yes |  |  |
| `log_loss` | real | yes |  |  |
| `rmse` | real | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `scheduler_profiles_scope_live` UNIQUE (`scope`) WHERE `deleted_at IS NULL`

Checks:

- `scheduler_profiles_w_json`: `json_valid(w) AND json_type(w) = 'array'`
- `scheduler_profiles_learning_steps_json`: `json_valid(learning_steps) AND json_type(learning_steps) = 'array'`
- `scheduler_profiles_relearning_steps_json`: `json_valid(relearning_steps) AND json_type(relearning_steps) = 'array'`
- `scheduler_profiles_enable_fuzz_bool`: `enable_fuzz IN (0, 1)`
- `scheduler_profiles_enable_short_term_bool`: `enable_short_term IN (0, 1)`
- `scheduler_profiles_maximum_interval_positive`: `maximum_interval >= 1`
- `scheduler_profiles_day_start_hour_range`: `day_start_hour >= 0 AND day_start_hour <= 23`
- `scheduler_profiles_n_reviews_nonnegative`: `n_reviews IS NULL OR n_reviews >= 0`
- `scheduler_profiles_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `scheduler_profiles_version_positive`: `version >= 1`
- `scheduler_profiles_updated_after_created`: `updated_at >= created_at`

### `knowledge_items`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `lesson_id` | text | yes |  | → `lessons.id` |
| `topic_id` | text | yes |  |  |
| `kind` | text | no |  |  |
| `fields` | text | no |  |  |
| `source_id` | text | yes |  | → `sources.id` |
| `annotation_id` | text | yes |  | → `annotations.id` |
| `locator` | text | yes |  |  |
| `as_of` | text | yes |  |  |
| `importance` | text | no | `'normal'` |  |
| `status` | text | no | `'need_to_learn'` |  |
| `created_by` | text | no | `'user'` |  |
| `tags` | text | no | `'[]'` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `knowledge_items_status_importance` (`status`, `importance`)
- `knowledge_items_topic` (`topic_id`)
- `knowledge_items_annotation` (`annotation_id`)
- `knowledge_items_source` (`source_id`)
- `knowledge_items_lesson` (`lesson_id`)

Checks:

- `knowledge_items_kind`: `kind IN ('fact', 'concept', 'procedure', 'principle', 'example', 'misconception', 'vocabulary', 'other')`
- `knowledge_items_importance`: `importance IN ('urgent', 'high', 'normal', 'maintenance', 'paused')`
- `knowledge_items_status`: `status IN ('need_to_learn', 'active', 'archived')`
- `knowledge_items_created_by`: `created_by IN ('user', 'ai', 'import')`
- `knowledge_items_fields_json`: `json_valid(fields) AND json_type(fields) = 'object'`
- `knowledge_items_locator_json`: `locator IS NULL OR (json_valid(locator) AND json_type(locator) = 'object')`
- `knowledge_items_tags_json`: `json_valid(tags) AND json_type(tags) = 'array'`
- `knowledge_items_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `knowledge_items_version_positive`: `version >= 1`
- `knowledge_items_updated_after_created`: `updated_at >= created_at`

### `cards`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `item_id` | text | no |  | → `knowledge_items.id` |
| `template` | text | no |  |  |
| `payload` | text | yes |  |  |
| `due` | integer | no |  |  |
| `stability` | real | no | `0` |  |
| `difficulty` | real | no | `0` |  |
| `scheduled_days` | integer | no | `0` |  |
| `learning_steps` | integer | no | `0` |  |
| `reps` | integer | no | `0` |  |
| `lapses` | integer | no | `0` |  |
| `state` | integer | no | `0` |  |
| `last_review` | integer | yes |  |  |
| `suspended` | integer | no | `0` |  |
| `buried_until` | integer | yes |  |  |
| `leech` | integer | no | `0` |  |
| `importance_override` | text | yes |  |  |
| `exam_id` | text | yes |  | → `exams.id` |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `cards_item_template_live` UNIQUE (`item_id`, `template`) WHERE `deleted_at IS NULL`
- `cards_state` (`state`)
- `cards_exam` (`exam_id`)
- `cards_item` (`item_id`)
- `cards_due` (`due`) WHERE `suspended = 0 AND deleted_at IS NULL`

Checks:

- `cards_state`: `state IN (0, 1, 2, 3)`
- `cards_suspended_bool`: `suspended IN (0, 1)`
- `cards_leech_bool`: `leech IN (0, 1)`
- `cards_importance_override`: `importance_override IS NULL OR importance_override IN ('urgent', 'high', 'normal', 'maintenance', 'paused')`
- `cards_stability_nonnegative`: `stability >= 0`
- `cards_difficulty_range`: `difficulty >= 0 AND difficulty <= 10`
- `cards_scheduled_days_nonnegative`: `scheduled_days >= 0`
- `cards_learning_steps_nonnegative`: `learning_steps >= 0`
- `cards_reps_nonnegative`: `reps >= 0`
- `cards_lapses_nonnegative`: `lapses >= 0`
- `cards_payload_json`: `payload IS NULL OR (json_valid(payload) AND json_type(payload) = 'object')`
- `cards_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `cards_version_positive`: `version >= 1`
- `cards_updated_after_created`: `updated_at >= created_at`

## Sessions, attempts and review log

What the user did: lesson sessions, activity attempts and the append-only FSRS review log (`src/schema/sessions.ts`).

### `lesson_sessions`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `lesson_id` | text | no |  | → `lessons.id` |
| `status` | text | no | `'in_progress'` |  |
| `started_at` | integer | no |  |  |
| `finished_at` | integer | yes |  |  |
| `duration_ms` | integer | yes |  |  |
| `xp` | integer | no | `0` |  |
| `accuracy` | real | yes |  |  |
| `activities_total` | integer | no | `0` |  |
| `activities_correct` | integer | no | `0` |  |
| `summary` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `lesson_sessions_started` (`started_at`)
- `lesson_sessions_lesson` (`lesson_id`)

Checks:

- `lesson_sessions_status`: `status IN ('in_progress', 'completed', 'abandoned')`
- `lesson_sessions_xp_nonnegative`: `xp >= 0`
- `lesson_sessions_accuracy_range`: `accuracy IS NULL OR (accuracy >= 0 AND accuracy <= 1)`
- `lesson_sessions_duration_nonnegative`: `duration_ms IS NULL OR duration_ms >= 0`
- `lesson_sessions_counts`: `activities_total >= 0 AND activities_correct >= 0 AND activities_correct <= activities_total`
- `lesson_sessions_finished_after_started`: `finished_at IS NULL OR finished_at >= started_at`
- `lesson_sessions_summary_json`: `summary IS NULL OR (json_valid(summary) AND json_type(summary) = 'object')`
- `lesson_sessions_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `lesson_sessions_version_positive`: `version >= 1`
- `lesson_sessions_updated_after_created`: `updated_at >= created_at`

### `attempts`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `activity_id` | text | no |  | → `activities.id` |
| `context` | text | no |  |  |
| `lesson_session_id` | text | yes |  | → `lesson_sessions.id` |
| `exam_attempt_id` | text | yes |  | → `exam_attempts.id` |
| `card_id` | text | yes |  | → `cards.id` |
| `started_at` | integer | no |  |  |
| `finished_at` | integer | yes |  |  |
| `score` | real | yes |  |  |
| `correct` | integer | yes |  |  |
| `rating` | integer | yes |  |  |
| `answer` | text | yes |  |  |
| `feedback` | text | yes |  |  |
| `time_ms` | integer | yes |  |  |
| `tries` | integer | no | `1` |  |
| `hints_used` | integer | no | `0` |  |
| `confidence` | text | yes |  |  |
| `ai_eval_call_id` | text | yes |  | → `ai_calls.id` |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `attempts_started` (`started_at`)
- `attempts_card` (`card_id`)
- `attempts_exam_attempt` (`exam_attempt_id`)
- `attempts_session` (`lesson_session_id`)
- `attempts_activity` (`activity_id`)

Checks:

- `attempts_context`: `context IN ('lesson', 'review', 'reinforcement', 'exam', 'diagnostic', 'remediation')`
- `attempts_confidence`: `confidence IS NULL OR confidence IN ('sure', 'unsure', 'guessed')`
- `attempts_score_range`: `score IS NULL OR (score >= 0 AND score <= 1)`
- `attempts_correct_bool`: `correct IS NULL OR correct IN (0, 1)`
- `attempts_rating`: `rating IS NULL OR rating IN (0, 1, 2, 3, 4)`
- `attempts_time_nonnegative`: `time_ms IS NULL OR time_ms >= 0`
- `attempts_tries_positive`: `tries >= 1`
- `attempts_hints_nonnegative`: `hints_used >= 0`
- `attempts_finished_after_started`: `finished_at IS NULL OR finished_at >= started_at`
- `attempts_answer_json`: `answer IS NULL OR json_valid(answer)`
- `attempts_feedback_json`: `feedback IS NULL OR json_valid(feedback)`
- `attempts_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `attempts_version_positive`: `version >= 1`
- `attempts_updated_after_created`: `updated_at >= created_at`

### `review_logs`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `card_id` | text | no |  | → `cards.id` |
| `rating` | integer | no |  |  |
| `state` | integer | no |  |  |
| `due` | integer | no |  |  |
| `stability` | real | no |  |  |
| `difficulty` | real | no |  |  |
| `elapsed_days` | integer | no |  |  |
| `scheduled_days` | integer | no |  |  |
| `learning_steps` | integer | no |  |  |
| `review` | integer | no |  |  |
| `duration_ms` | integer | yes |  |  |
| `context` | text | no |  |  |
| `exercise_score` | real | yes |  |  |
| `device` | text | yes |  |  |
| `attempt_id` | text | yes |  | → `attempts.id` |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `rl_attempt` (`attempt_id`)
- `rl_review` (`review`)
- `rl_card` (`card_id`, `review`)

Checks:

- `review_logs_rating`: `rating IN (0, 1, 2, 3, 4)`
- `review_logs_state`: `state IN (0, 1, 2, 3)`
- `review_logs_context`: `context IN ('daily', 'lesson', 'reinforcement', 'exam_sim', 'cram', 'manual_postpone', 'import')`
- `review_logs_stability_nonnegative`: `stability >= 0`
- `review_logs_difficulty_range`: `difficulty >= 0 AND difficulty <= 10`
- `review_logs_elapsed_days_nonnegative`: `elapsed_days >= 0`
- `review_logs_scheduled_days_nonnegative`: `scheduled_days >= 0`
- `review_logs_learning_steps_nonnegative`: `learning_steps >= 0`
- `review_logs_duration_nonnegative`: `duration_ms IS NULL OR duration_ms >= 0`
- `review_logs_exercise_score_range`: `exercise_score IS NULL OR (exercise_score >= 0 AND exercise_score <= 1)`
- `review_logs_append_only`: `updated_at = created_at AND version = 1`
- `review_logs_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `review_logs_version_positive`: `version >= 1`
- `review_logs_updated_after_created`: `updated_at >= created_at`

## Infrastructure

Job queue, AI cost log, settings and the (v1-empty) sync outbox (`src/schema/system.ts`).

### `jobs`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `kind` | text | no |  |  |
| `status` | text | no | `'queued'` |  |
| `priority` | integer | no | `0` |  |
| `payload` | text | no |  |  |
| `result` | text | yes |  |  |
| `progress` | text | yes |  |  |
| `attempts` | integer | no | `0` |  |
| `max_attempts` | integer | no | `3` |  |
| `run_after` | integer | no |  |  |
| `locked_by` | text | yes |  |  |
| `locked_at` | integer | yes |  |  |
| `started_at` | integer | yes |  |  |
| `finished_at` | integer | yes |  |  |
| `error` | text | yes |  |  |
| `parent_job_id` | text | yes |  | → `jobs.id` |
| `subject_id` | text | yes |  |  |
| `idempotency_key` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `jobs_idempotency_key_live` UNIQUE (`idempotency_key`) WHERE `idempotency_key IS NOT NULL AND deleted_at IS NULL`
- `jobs_parent` (`parent_job_id`)
- `jobs_subject` (`subject_id`)
- `jobs_queue` (`status`, `run_after`, `priority`)

Checks:

- `jobs_status`: `status IN ('queued', 'running', 'succeeded', 'failed', 'cancelled')`
- `jobs_attempts_nonnegative`: `attempts >= 0`
- `jobs_max_attempts_positive`: `max_attempts >= 1`
- `jobs_payload_json`: `json_valid(payload) AND json_type(payload) = 'object'`
- `jobs_result_json`: `result IS NULL OR json_valid(result)`
- `jobs_progress_json`: `progress IS NULL OR (json_valid(progress) AND json_type(progress) = 'object')`
- `jobs_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `jobs_version_positive`: `version >= 1`
- `jobs_updated_after_created`: `updated_at >= created_at`

### `ai_calls`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `provider` | text | no |  |  |
| `model` | text | no |  |  |
| `role` | text | yes |  |  |
| `purpose` | text | no |  |  |
| `status` | text | no | `'ok'` |  |
| `input_tokens` | integer | no | `0` |  |
| `output_tokens` | integer | no | `0` |  |
| `cached_input_tokens` | integer | no | `0` |  |
| `reasoning_tokens` | integer | no | `0` |  |
| `cost_usd` | real | no | `0` |  |
| `latency_ms` | integer | yes |  |  |
| `batch_id` | text | yes |  |  |
| `custom_id` | text | yes |  |  |
| `prompt_version` | text | yes |  |  |
| `schema_version` | text | yes |  |  |
| `temperature` | real | yes |  |  |
| `job_id` | text | yes |  | → `jobs.id` |
| `error` | text | yes |  |  |
| `meta` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `ai_calls_custom_id` (`custom_id`)
- `ai_calls_job` (`job_id`)
- `ai_calls_provider_model` (`provider`, `model`)
- `ai_calls_created` (`created_at`)

Checks:

- `ai_calls_status`: `status IN ('ok', 'error')`
- `ai_calls_input_tokens_nonnegative`: `input_tokens >= 0`
- `ai_calls_output_tokens_nonnegative`: `output_tokens >= 0`
- `ai_calls_cached_tokens_nonnegative`: `cached_input_tokens >= 0`
- `ai_calls_reasoning_tokens_nonnegative`: `reasoning_tokens >= 0`
- `ai_calls_cost_nonnegative`: `cost_usd >= 0`
- `ai_calls_latency_nonnegative`: `latency_ms IS NULL OR latency_ms >= 0`
- `ai_calls_temperature_range`: `temperature IS NULL OR (temperature >= 0 AND temperature <= 2)`
- `ai_calls_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `ai_calls_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `ai_calls_version_positive`: `version >= 1`
- `ai_calls_updated_after_created`: `updated_at >= created_at`

### `settings`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `key` | text | no |  |  |
| `value` | text | no |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `settings_key_live` UNIQUE (`key`) WHERE `deleted_at IS NULL`

Checks:

- `settings_key_nonempty`: `length(key) > 0`
- `settings_value_json`: `json_valid(value)`
- `settings_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `settings_version_positive`: `version >= 1`
- `settings_updated_after_created`: `updated_at >= created_at`

### `outbox`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `table_name` | text | no |  |  |
| `row_id` | text | no |  |  |
| `op` | text | no |  |  |
| `row_version` | integer | no |  |  |
| `payload` | text | yes |  |  |
| `synced_at` | integer | yes |  |  |
| `attempts` | integer | no | `0` |  |
| `error` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `outbox_row` (`table_name`, `row_id`)
- `outbox_pending` (`synced_at`, `created_at`)

Checks:

- `outbox_op`: `op IN ('insert', 'update', 'delete')`
- `outbox_row_version_positive`: `row_version >= 1`
- `outbox_attempts_nonnegative`: `attempts >= 0`
- `outbox_payload_json`: `payload IS NULL OR (json_valid(payload) AND json_type(payload) = 'object')`
- `outbox_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `outbox_version_positive`: `version >= 1`
- `outbox_updated_after_created`: `updated_at >= created_at`

## Gamification

XP ledger, streaks and achievements (`src/schema/gamification.ts`).

### `xp_events`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `amount` | integer | no |  |  |
| `reason` | text | no |  |  |
| `subject_kind` | text | yes |  |  |
| `subject_id` | text | yes |  |  |
| `occurred_at` | integer | no |  |  |
| `multiplier` | real | no | `1` |  |
| `meta` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `xp_events_subject` (`subject_kind`, `subject_id`)
- `xp_events_occurred` (`occurred_at`)

Checks:

- `xp_events_reason`: `reason IN ('lesson', 'review', 'reinforcement', 'mock_exam', 'quest', 'achievement', 'bonus')`
- `xp_events_amount_nonnegative`: `amount >= 0`
- `xp_events_multiplier_positive`: `multiplier > 0`
- `xp_events_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `xp_events_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `xp_events_version_positive`: `version >= 1`
- `xp_events_updated_after_created`: `updated_at >= created_at`

### `streaks`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `kind` | text | no |  |  |
| `current_length` | integer | no | `0` |  |
| `longest_length` | integer | no | `0` |  |
| `goal` | integer | no | `10` |  |
| `last_active_day` | text | yes |  |  |
| `started_on` | text | yes |  |  |
| `freezes_available` | integer | no | `0` |  |
| `freezes_used` | integer | no | `0` |  |
| `freeze_bank_max` | integer | no | `2` |  |
| `holidays` | text | no | `'[]'` |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `streaks_kind_live` UNIQUE (`kind`) WHERE `deleted_at IS NULL`

Checks:

- `streaks_current_nonnegative`: `current_length >= 0`
- `streaks_longest_nonnegative`: `longest_length >= 0`
- `streaks_goal_positive`: `goal >= 1`
- `streaks_freezes_range`: `freezes_available >= 0 AND freezes_available <= 1000`
- `streaks_freezes_used_nonnegative`: `freezes_used >= 0`
- `streaks_freeze_bank_nonnegative`: `freeze_bank_max >= 0`
- `streaks_holidays_json`: `json_valid(holidays) AND json_type(holidays) = 'array'`
- `streaks_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `streaks_version_positive`: `version >= 1`
- `streaks_updated_after_created`: `updated_at >= created_at`

### `achievements`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `id` | text | no |  | PK |
| `key` | text | no |  |  |
| `progress` | real | no | `0` |  |
| `target` | real | no | `1` |  |
| `unlocked_at` | integer | yes |  |  |
| `meta` | text | yes |  |  |
| `created_at` | integer | no |  |  |
| `updated_at` | integer | no |  |  |
| `deleted_at` | integer | yes |  |  |
| `device_id` | text | no |  |  |
| `version` | integer | no | `1` |  |

Indexes:

- `achievements_key_live` UNIQUE (`key`) WHERE `deleted_at IS NULL`

Checks:

- `achievements_key_nonempty`: `length(key) > 0`
- `achievements_progress_nonnegative`: `progress >= 0`
- `achievements_target_positive`: `target > 0`
- `achievements_meta_json`: `meta IS NULL OR (json_valid(meta) AND json_type(meta) = 'object')`
- `achievements_id_uuidv7`: `length(id) = 36 AND substr(id, 15, 1) = '7'`
- `achievements_version_positive`: `version >= 1`
- `achievements_updated_after_created`: `updated_at >= created_at`

## Migration bookkeeping

Owned by `src/migrator.ts`, created on first open. The one table without the UUIDv7/audit set: it describes the schema, is never synced, and must exist before any migration runs.

### `_migrations`

| Column | Type | Null | Default | Key |
|---|---|---|---|---|
| `name` | text | no |  | PK |
| `hash` | text | no |  |  |
| `applied_at` | integer | no |  |  |
| `duration_ms` | integer | no |  |  |
